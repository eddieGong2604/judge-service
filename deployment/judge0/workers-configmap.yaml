apiVersion: v1
kind: ConfigMap
metadata:
  name: workers-config
  labels:
    io.kompose.service: workers
data:
  RAILS_MAX_THREADS: "50"
  RAILS_SERVER_PROCESSES: "50"
  MAX_QUEUE_SIZE: "1000"
  COUNT: "50"
